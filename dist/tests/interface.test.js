describe('Interface', () => {
    it("Should be support in typescript", () => {
        const seller = {
            id: 1,
            name: "Samsung",
            address: "Korea"
        };
        console.info(seller);
    });
});
export {};
