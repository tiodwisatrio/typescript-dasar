"use strict";
describe("tipe data", () => {
  it("should be string", () => {
    const name = "Tio Dwi Satrio";
    const balance = 1000000;
    const isVip = true;

    console.info(name);
    console.info(balance);
    console.info(isVip);
  });
});
