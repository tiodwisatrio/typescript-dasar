describe("tipe data", () => {
    it("should be string", () => {
        const name: string = "Tio Dwi Satrio";
        const balance: number = 1000000;
        const isVip: boolean = true;

        console.info(name)
        console.info(balance)
        console.info(isVip)
    })
})